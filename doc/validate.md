# Bean Validationを使ったバリデーション

- コントローラに`@Valid`をつける
- コントローラのdtoに`@NotBlank`等をつけると効く。

---

## バリデーションの考え方

- 出力時エスケープ(XSS対策等): テンプレートエンジン/フロント- アプリケーションロジックとして不正な値: Bean Validation(コントロラー層)
- 特殊文字チェック(SQLI等): リポジトリ層で使用するライブラリ等
- 値オブジェクト: 上記とは別でバイパスして変更される対策で実施
